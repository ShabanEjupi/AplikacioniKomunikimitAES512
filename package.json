{
  "name": "crypto-512-root",
  "version": "1.0.0",
  "description": "Crypto 512 - Secure communication app",
  "private": true,
  "scripts": {
    "build": "cd client && npm install --legacy-peer-deps && npm run build",
    "start": "cd client && npm start"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.9.4"
  },
  "devDependencies": {
    "typescript": "4.9.5"
  }
}
