{
  "name": "crypto-512-root",
  "version": "1.0.0",
  "description": "Crypto 512 - Aplikacioni i komunikimit të sigurt me ASH-512 hash dhe koordinata gjeometrike",
  "private": true,
  "scripts": {
    "build": "node build-simple-direct.js",
    "build:shared": "cd shared && npm install && npx tsc -p tsconfig.json",
    "build:server": "cd server && npm install && npm run build",
    "build:client": "cd client && npm install && npm run build",
    "test": "jest",
    "test:watch": "jest --watch",
    "install:all": "npm install --legacy-peer-deps && npm install --legacy-peer-deps --workspaces",
    "start:server": "cd server && npm start",
    "start:client": "cd client && npm start",
    "start:demo": "concurrently \"npm run start:server\" \"npm run start:client\"",
    "dev": "npm run start:demo",
    "demo:ash512": "cd scripts && npx ts-node ash512-performance.ts"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.9.4"
  },
  "devDependencies": {
    "@jest/globals": "^29.0.0",
    "@types/jest": "^29.0.0",
    "@types/node": "^18.0.0",
    "jest": "^29.0.0",
    "ts-jest": "^29.0.0",
    "ts-node": "^10.9.2",
    "typescript": "4.9.5",
    "concurrently": "^7.6.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "resolutions": {
    "typescript": "4.9.5"
  },
  "overrides": {
    "typescript": "4.9.5"
  },

  "keywords": [
    "secure",
    "communication",
    "authentication",
    "encryption"
  ],
  "author": "Shaban Ejupi",
  "license": "MIT"
}
