{
  "name": "crypto-512-shared",
  "version": "1.0.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "echo 'Skipping TypeScript build for deployment' && exit 0",
    "build:tsc": "npx tsc -p tsconfig.json",
    "build:full": "npx tsc -p tsconfig.json",
    "watch": "npx tsc -p tsconfig.json --watch"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "typescript": "^5.0.0"
  }
}
